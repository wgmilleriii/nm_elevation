{
  "name": "nm-elevation",
  "version": "1.0.0",
  "type": "module",
  "description": "New Mexico elevation data collection system",
  "main": "collect_sparse_points.js",
  "scripts": {
    "start": "node collect_sparse_points.js",
    "clean-locks": "node -e \"import('./db_manager.js').then(({cleanupStaleLocks}) => cleanupStaleLocks())\""
  },
  "dependencies": {
    "better-sqlite3": "^9.4.0",
    "node-fetch": "^3.3.2"
  }
}
